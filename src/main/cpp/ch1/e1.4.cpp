#include <iostream>

int main() {
    std::cout << "Enter two numbers: " << std::endl;

    float x, y;
    std::cin >> x >> y;

    std::cout << "Product of " << x << " and " << y << " is " << x * y << std::endl;

    return 0;
}
